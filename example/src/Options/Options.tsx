import React from 'react';
import { Form } from 'antd';

const Options = () => {
	return (
		<div>
			<Form>
				<Form.Item>
					
				</Form.Item>
			</Form>
		</div>
	);
};

export default Options;
